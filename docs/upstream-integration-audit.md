# 上游历史整合记录

## 1.0.32：上游 1.0.31–1.0.32 增量整合

- 基准 `a8c7863` 至 `edfacd9`：`30b39e1` Linux 渲染修复、`bed9968` MASQUE/双副内核、`b09095f` 窗口缩放记忆、`5b2d89f` 图标、`483545a` Windows 原生 API 已适配整合。
- `9a4886e` 默认打包目标收窄已纳入；不采用上游文档中的三内核强制捆绑或通用安全解除提示。
- `fc2e0c3`、`edfacd9` 版本提交按分支构建链同步到 1.0.32，不覆盖既有发布记录或同名上游标签。
- `c3ec90d` 纯格式化不整文件覆盖分支；保持功能修改可追踪。`a5ed33d`、`29b3cc3` 的 Android 独立项目、Linux 支持说明按本分支实际发布能力在 README 注明，不虚构本分支已有 Linux 包。
- 保留原生 Mihomo 订阅策略、IPv4/DNS、节点拖拽/右键、确认安全与 UWP 行为。不恢复 TUN 感知提示或赞助入口。
- 对上游剪贴板 API 修复追加有效窗口所有者和分配失败保护；对新增窗口缩放追加节点拖拽坐标适配。
- 本轮验证结果另记；历史 481 项测试结果不代表当前提交验证完成。

## 2026-09-12 增量核对：1.0.29–1.0.30

最终验证：`c0dcb4a` / Actions `34669437435` 成功；Rust 481 通过、0 失败、4 跳过；确认弹窗 2 通过。前两轮分别发现并修正接口迁移/所有权编译错误和新增测试样例 ID 重复问题。失败构建未发布。未执行本地客户端安装或 GUI 实机验收。

- 上游 `fa37f6c`、`7617ab1`、`08f1cff` 的规则集独立 DNS、指定多节点池、编辑器、快捷菜单、悬停提示与跳过节点报告已适配整合。
- `13726ca`、`568dee8`、`90d0bdc` 的导航栏与光斑修正已合入。
- `8e86c96`、`a8c7863` 版本提交不直接覆盖分支：当前仍为 1.0.28，Windows 内核版本标记与实际已捆绑文件一致，不只改标记冒充更新二进制。
- 保留分支官方内核下载、稳定/预览选择、安装时间、短链回退、节点右键及拖拽、确认安全修复；未引入上游与这些功能冲突的旧实现。
- Mihomo 不恢复本地 sing-box 规则编译，订阅原生策略优先；无原生策略时生成远程分类可使用指定池。
- 新增远程池成员、内部组重名与失效节点回退测试。验证状态以本次 Actions 结果为准；下方旧构建记录不是本次验证结论。

核对日期：2026-09-11。上游基准：`849e288`（1.0.28）。
本次修复基线：`b35ae6f`。应用版本继续使用 1.0.28。

## 范围与判定

- 1.0.8、1.0.9 标签均已进入分支祖先历史；不能据此推断后续没有回归。
- 共同基线之后核对了 112 个非合并提交记录，并对已确认的遗漏逐文件补齐。
- 1.0.22、1.0.26 有标签但没有独立 Release 页面，已纳入范围。
- 本表是功能级追踪，不表示所有历史提交都逐字合并，也不表示所有平台已经实机验证。

| 上游版本 | 处理结果 |
|---|---|
| 1.0.8–1.0.9 | 保留现有 TUN、规则、三内核、看门狗及 Xray 出口同步实现 |
| 1.0.10 | 补入代理链画布与菜单（前端）；补入首页视觉偏好缓存和首屏背景；保留原有首页默认风格、后端代理链和诊断接口 |
| 1.0.11 | 补齐 Xray 分类池与唯一兜底、Mihomo 禁止跨池 fallback、共享国内 DNS 池；补齐 macOS 授权诊断及 PTY 超时进程组清理 |
| 1.0.12–1.0.14 | 保留已有多核委托、流式测速、状态事件、快捷键、出口探测与日志实现；未宣称所有界面细节逐字一致 |
| 1.0.15–1.0.18 | 保留已有 TUN 等待、回环排除、sing-box 版本迁移、分组及列表修复；Linux 发布流程不在本分支当前发布范围 |
| 1.0.19–1.0.21 | 保留托盘常用操作、低内存入口、标题栏跟随和规则菜单修复 |
| 1.0.22–1.0.23 | 保留收藏、旧节点 ID 迁移、删除边界、实时排序及本分支右键操作 |
| 1.0.24 | 补齐 Xray 委托标签对应的页面布局容器，不覆盖本分支列宽与圆点选节点行为 |
| 1.0.25 | 保留自定义订阅 User-Agent；本分支认证代理、短链及回退行为不变 |
| 1.0.26 | 补齐 macOS 脚本执行权限、自检容错和 GeoSite.dat 清单大小写；保留已合入的原位升级授权与下载进度 |
| 1.0.27 | 补齐 Intel macOS Mihomo compatible 预取包；不强制捆绑三内核 |
| 1.0.28 | 保留节点详情与自定义远程 DoH；补测试覆盖多条 Xray 远程解析器 |

## DNS 修复边界

- Mihomo 不再生成跨池 fallback；nameserver 仅使用用户选择的默认池。
- Xray 的其他池仅服务显式分类，使用 skipFallback；Local 分类不会再因默认选择远程而丢失。
- 旧 leak_protect 字段保留反序列化兼容，但不再关闭回退保护；已移除无效开关。
- 国内解析、节点域名引导解析仍允许按配置直连，不承诺系统级零 DNS 泄露。
- sing-box 保留本分支 direct_resolver、IPv4 和旧内核兼容逻辑，只将国内地址改用共享常量。
- Mihomo 的订阅原生策略组、规则优先级、模式选择和本分支直连出口不被上游生成器覆盖。

## 主动保留的差异

不恢复赞助入口、系统代理绕过/TUN 提示；不引入第三方内核下载镜像、
仓库内 geodata 二进制快照或强制三内核捆绑。保留 Windows 安装版和便携版。

## 验证

### 继续核对：智能切换（第三次构建）

对照上游 `4af2038` 及后续 `a386634`、`ef5f2e4`，发现状态层丢失了
`close_if_enabled` 约束；智能切换虽然计算了 `hard_fail`，却没有传递到连接清理路径。
现补回该参数，手动切换传 true，智能切换传 hard_fail，最终仍受用户设置控制。
后台策略组调整取消全局清理；手动 Mihomo 分组操作仍走本分支独立入口。
两种内核下覆盖手动、普通智能优选、故障恢复、禁用清理、后台分组共 12 个场景。

本轮还核对了流式测速、连接记录自适应轮询与出口探测：已存在对应实现，
不因注释、命名或排版差异重复覆盖。尚不能据此声称所有历史界面细节、所有平台均已验证。

以下 470 项通过的结果属于上一次构建；本轮新增测试结果以本轮 GitHub Actions 为准。

第三次构建已验证：`7a61770`，任务 `34572982544` 成功，471 通过、0 失败、4 跳过。
新增测试通过真实状态层连接本地模拟接口，覆盖两种内核下的 12 种场景。
此前 `caaafa8` 标签归档为 `archive/v1.0.28-caaafa8`。

本地仅安装前端依赖、运行类型检查、脚本语法与源码检查，不编译客户端。
Windows 编译、安装包、便携包与 Rust 单元测试由 GitHub Actions 执行。
macOS 修改来自对应上游实现，但仍需要 macOS 实机授权/打包验证。
本次不修改本地安装目录、订阅、系统代理或正在运行的内核。

本轮应用构建提交：`caaafa8`，GitHub Actions 任务 `34570520712`：
Windows 安装包与便携包成功；Rust 测试 470 通过、0 失败、4 跳过。
已测试产物可通过 publish-windows.yml 在 GitHub 内直接发布，避免本地中转下载故障。
旧版标签保留为 `archive/v1.0.28-b35ae6f`；同版本新发布以正文记录的构建 SHA 为准。
