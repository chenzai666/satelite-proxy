# 上游 v1.0.44 整合记录

核对日期：2026-09-25。比较上游正式版 v1.0.43（03b0bf6）至 v1.0.44（149d1ee9097b86481197655c2c86834ae7e99eb0）。没有用上游文件整体覆盖分支定制。

| 上游提交 | 内容 | 本分支处理 |
| --- | --- | --- |
| b71c610 | 冷启动、托盘唤醒恢复窗口大小与位置；屏幕外位置回正 | 合入，兼容旧版仅保存宽高的文件；保留窗口图标和主题定制 |
| e6cbb54 | Windows 图标改用手调 macOS 源图 | 合入生成脚本及 11 份 Windows 图标资产；保留托盘样式 |
| 149d1ee | 版本更新至 1.0.44 | 同步应用、Cargo 锁文件、版本检查及 Windows 构建/发布流程 |

## 分支补强

- 位置校验检查标题栏是否可拖动，不将仅露出下半部分的窗口视为可达。
- 保留 WebView 的托盘隐藏模式同样检查当前位置，显示器拔除后也能回正。
- 增加负坐标、多显示器边缘、标题栏屏外和非法数值的纯函数测试，保留上游布局读取测试。
- 保留节点页 Ctrl+C/Ctrl+V、当前配置追加、clone 副本、改名与分享链接编码修复。

## 仍未移植的历史差异

上游同 URL 多订阅及节点身份迁移仍按 1.0.43 整合记录暂缓，不覆盖本分支现有节点 ID、编辑覆盖、排序、规则引用和本地追加副本模型。

## 验证边界

本地仅做静态检查和前端纯函数测试；Rust 测试、客户端及安装包构建在 GitHub Actions 执行。不同 DPI 实际多显示器、macOS/Linux 窗口行为仍需实机验证，不能以纯函数测试代替。
