# 上游 v1.0.41–v1.0.43 选择性整合记录

基线为本分支 v1.0.40。上游正式标签 v1.0.43 对应 `03b0bf6`；本分支不直接合并上游分支，以保留本地代理、订阅和节点交互逻辑。

| 上游提交 | 内容 | 本分支处理 |
| --- | --- | --- |
| `a32b2a0` | 经典土星托盘与选择器布局 | 合入资源和选项，默认样式不变 |
| `78e8111`、`ffc56b5` | Windows 标题栏 DPI 图标和托盘左键交互 | 合入，保留本分支主窗口图标逻辑 |
| `2475cee` | TLS 分片、TUN 副核防回环、订阅 ID 展示 | 合入 TLS 和防回环；未覆盖节点/订阅 UI |
| `cdb189d`、`f81ac9c`、`2726961` | 最终冰蓝图标及 macOS 源图 | 合入最终版资源，不纳入历史候选图标 |
| `dbd8237` | VLESS packet_encoding 值校验 | 合入解析和生成路径 |
| `112cffe` | 端口残留等待缩短 | 按本分支运行模式适配后合入 |
| `a6c7da5` | 同 URL 多订阅、节点 ID 迁移、卡片徽标和编辑 | 暂不合入；本分支已有节点编辑与覆盖，身份迁移需独立验证 |

版本号统一为 1.0.43。发布产物以 GitHub Actions 对应标签构建成功及 Release 资产核对为准，不以源码版本号代替已发布状态。
