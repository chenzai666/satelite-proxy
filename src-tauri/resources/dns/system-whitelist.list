# Bundled system DNS whitelist.
# Bare lines  -> domain_suffix + SYSTEM
# CSV lines   -> MATCHER,payload,ACTION  (e.g. DOMAIN-SUFFIX,example.com,SYSTEM)
# Routes these domains to the local/system resolver instead of remote DoH.

# Local / internal
local
lan
internal
corp
localhost
*.local
*.lan
